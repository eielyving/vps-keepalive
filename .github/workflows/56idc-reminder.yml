name: 56idc VPS Login Reminder

on:
  schedule:
    - cron: '10 2 * * 1'   # 每周一 02:10 UTC = 北京时间 10:10
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest

    steps:
      - name: Send Telegram Reminder
        run: |
          TIME=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')

          cat <<'EOF' > message.txt
<b>🔔 56idc VPS 保活提醒</b>

你的 VPS <b>需要定期登录官网才能保持活跃状态</b>，否则可能会被系统判定为不活跃而回收。

👉 <b>请现在完成以下操作：</b>
1️⃣ 打开 56idc 官网  
2️⃣ 使用浏览器正常登录一次  
3️⃣ 登录成功后即可关闭页面（无需其他操作）

🔗 <b>登录入口（点击即可）：</b>
<a href="https://56idc.net/login">👉 点击登录 56idc 官网</a>

🕒 <b>提醒时间（北京时间）：</b>
EOF

          echo "$TIME" >> message.txt

          curl -s https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
            --data-urlencode chat_id=${{ secrets.TG_CHAT_ID }} \
            --data-urlencode parse_mode=HTML \
            --data-urlencode text@"message.txt"
