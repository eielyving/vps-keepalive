name: 56idc VPS Login Reminder

on:
  schedule:
    - cron: '10 2 * * 1'   # 每周一 02:10 UTC = 北京时间 10:10
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest

    steps:
      - name: Send Telegram Reminder
        run: |
          TIME=$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')
          MSG="🔔 56idc VPS 保活提醒\n\n请你手动登录一次官网以保持 VPS 活跃：\n👉 https://56idc.net/login\n\n🕒 北京时间：$TIME"

          curl -s -X POST https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TG_CHAT_ID }} \
            -d text="$MSG"
